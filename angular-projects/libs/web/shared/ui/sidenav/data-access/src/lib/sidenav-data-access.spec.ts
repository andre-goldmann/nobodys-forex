import { sidenavDataAccess } from './sidenav-data-access';

describe('sidenavDataAccess', () => {
  it('should work', () => {
    expect(sidenavDataAccess()).toEqual('sidenav-data-access');
  });
});
