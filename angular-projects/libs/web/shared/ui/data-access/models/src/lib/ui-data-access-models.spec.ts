import { uiDataAccessModels } from './ui-data-access-models';

describe('uiDataAccessModels', () => {
  it('should work', () => {
    expect(uiDataAccessModels()).toEqual('ui-data-access-models');
  });
});
