import { forexAppDataAccess } from './forex-app-data-access';

describe('forexAppDataAccess', () => {
  it('should work', () => {
    expect(forexAppDataAccess()).toEqual('forex-app-data-access');
  });
});
